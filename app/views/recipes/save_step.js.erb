$("[data-form=edit-step]").on("ajax:success", function(event){
  var step = $(event.target).closest("[data-li=step]");
  $(step)
  .find("[data-form=edit-step]").hide();
  $(step)
  .find("[data-div=step-info]")
  .html("<%= j render(:partial => 'info_step', locals: {step: @step}) %>")
});
