<% conversation.participants.each do |participant| %>
  <% unless participant == current_dreamer %>
    <%= link_to image_tag("#{participant.avatar_url}", class: "icon"), profile_path(participant) %>
  <% end %>
<% end %>