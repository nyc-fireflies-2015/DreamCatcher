<p>
  <div class="dream-info">
    <%= link_to "#{dream.dreamer.username}", profile_path(dream.dreamer)%>

    <div class="right">
      posted <%= dream.timestamp %> ago
    </div>
      <div class="favorites">
        <div data-favorite="new">
      <%= render partial: "favorite", locals: {dream: dream} %>
    </div>
    <div data-favorite="destroy">
    <%= render partial: "unfavorite", locals: {dream: dream} %>
  </div>
  </div>
  </div>
</p>

<%= dream.story %><br>
<sub class="right"><%= link_to "read more >>", dream_path(dream) %></sub>
<hr>
