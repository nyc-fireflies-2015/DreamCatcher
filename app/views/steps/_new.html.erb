<div data-div="step-form">
  <h1>Create New Step</h1>

  <%= form_for step, url: step_index_path, remote: true do |f| %>
    <p><%= f.text_area :description, placeholder: "description" %></p>
    <p>
      <div data-handler="errors"></div>
      <%= f.submit "Submit" %>
    </p>
  <% end %>
</div>
