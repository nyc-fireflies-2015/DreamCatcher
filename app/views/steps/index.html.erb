<div class="popular-steps-container">
  <div data-form="new_step">
   <%= render partial: "new", locals: {step: @new_step} %>
  </div>

  <div class="center">
    <p><%= link_to "Add to Recipe", new_step_path, class: "button", data: {link: "new_step" }, remote: true %></p>
    <h3>Popular Steps</h3>
  </div>

  <div class="popular-steps-river">
    <ul data-river="popular_steps">
      <% @popular_steps.each do |step| %>
        <%= render partial: "step", locals: {step: step} %>
      <% end %>
    </ul>
  </div>
</div>

<div class="recipe-container">
  <div class="center">
    <h3>My Dream Recipe</h3>
  </div>

  <div class="recipe-steps-river">
    <ul data-river="recipe_steps">
    <% @steps.each do |step| %>
      <%= render partial: "step", locals: {step: step} %>
    <% end %>
    </ul>
  </div>
</div>
